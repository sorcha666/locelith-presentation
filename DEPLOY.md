# Replace YOUR_GITHUB_USERNAME with your actual GitHub username
# Run these 2 commands in PowerShell inside the project folder:

git remote add origin https://github.com/YOUR_GITHUB_USERNAME/locelith-presentation.git
git push -u origin master
